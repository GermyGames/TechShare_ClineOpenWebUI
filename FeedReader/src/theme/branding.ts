import { colors } from './colors';

export const branding = {
  primaryColor: colors.mainPurple,
  logo: null, // Replace with logo path or component if available
  // Add more branding properties as needed
};